<li>
    {{book_name}}

    <button class="change-read btn btn-warning" data-id="{{id}}" data-newread="{{wasRead}}">
        {{#if wasRead}}I read it{{else}}Read Again{{/if}}
    </button>

    <button class="delete-btn btn btn-danger" data-id="{{this.id}}">DELETE!</button>
</li>